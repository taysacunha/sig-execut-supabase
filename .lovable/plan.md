Remover o indicador `+N outro(s)` da coluna "Sistemas" em `src/pages/UserManagement.tsx`. Os badges dos demais módulos já são renderizados (em variante `outline` quando há filtro), então o contador é redundante e sugere módulos ocultos que não existem.

Mudança única: deletar o bloco `{moduleFilter !== "all" && user.systems.filter(...).length > 0 && (<Tooltip>...+N outro(s)...</Tooltip>)}` na célula da coluna Sistemas. Manter o restante (destaque do módulo filtrado, badges outline dos demais).