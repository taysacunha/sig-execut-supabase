# Roadmap

- [ ] Auditar a diferença real entre `dev_tracker` e `dev_tracker_log`.
- [ ] Reconciliar o histórico sem perder horas do consolidado.
- [ ] Usar `dev_tracker_log` como fonte única nas visões por sistema e cronológica.
- [ ] Unificar totais e PDFs e validar a página `/dev`.
